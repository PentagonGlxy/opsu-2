diff --git a/src/fluddokt/ex/DevicePerformance.java b/src/fluddokt/ex/DevicePerformance.java
new file mode 100644
index 00000000..180704d7
--- /dev/null
+++ b/src/fluddokt/ex/DevicePerformance.java
@@ -0,0 +1,14 @@
+package fluddokt.ex;
+
+/**
+ * Platform bridge for runtime performance and power-management hints.
+ */
+public class DevicePerformance {
+	public static DevicePerformance info = new DevicePerformance();
+
+	public void setBatterySaverEnabled(boolean enabled) {
+	}
+
+	public void setGameplayActive(boolean active) {
+	}
+}
diff --git a/src/fluddokt/opsu/fake/GameOpsu.java b/src/fluddokt/opsu/fake/GameOpsu.java
index 19192803..c83b1846 100644
--- a/src/fluddokt/opsu/fake/GameOpsu.java
+++ b/src/fluddokt/opsu/fake/GameOpsu.java
@@ -26,7 +26,7 @@ import com.badlogic.gdx.utils.viewport.ScreenViewport;
 
 public class GameOpsu extends com.badlogic.gdx.Game {
 
-	public final static String VERSION = "0.17.0-alpha1";
+	public final static String VERSION = "1.0.0";
 	public StateBasedGame sbg;
 	
 	Stage stage;
diff --git a/src/itdelatrisu/opsu/Utils.java b/src/itdelatrisu/opsu/Utils.java
index dcca6bfb..c11cbc51 100644
--- a/src/itdelatrisu/opsu/Utils.java
+++ b/src/itdelatrisu/opsu/Utils.java
@@ -18,6 +18,7 @@
 
 package itdelatrisu.opsu;
 
+import fluddokt.ex.DevicePerformance;
 import fluddokt.opsu.fake.*;
 import com.badlogic.gdx.Gdx;
 
@@ -138,6 +139,7 @@ public class Utils {
 		int height = container.getHeight();
 
 		// game settings
+		DevicePerformance.info.setBatterySaverEnabled(Options.isBatterySaverEnabled());
 		container.setTargetFrameRate(Options.getTargetFPS());
 		container.setVSync(Options.getTargetFPS() == 60);
 		container.setMusicVolume(Options.getMusicVolume() * Options.getMasterVolume());
diff --git a/src/itdelatrisu/opsu/options/OptionGroup.java b/src/itdelatrisu/opsu/options/OptionGroup.java
index f0e0e16a..c338514e 100644
--- a/src/itdelatrisu/opsu/options/OptionGroup.java
+++ b/src/itdelatrisu/opsu/options/OptionGroup.java
@@ -55,6 +55,8 @@ public class OptionGroup {
 				(com.badlogic.gdx.Gdx.app.getType() == ApplicationType.Desktop) ? 
 						GameOption.TARGET_FPS : GameOption.NULL,
 			GameOption.SHOW_FPS,
+			(com.badlogic.gdx.Gdx.app.getType() == ApplicationType.Android) ?
+					GameOption.BATTERY_SAVER : GameOption.NULL,
 		}),
 		new OptionGroup("DETAIL SETTINGS", new GameOption[] {
 			GameOption.SNAKING_SLIDERS,
diff --git a/src/itdelatrisu/opsu/options/Options.java b/src/itdelatrisu/opsu/options/Options.java
index 500a37ce..085a8d2f 100644
--- a/src/itdelatrisu/opsu/options/Options.java
+++ b/src/itdelatrisu/opsu/options/Options.java
@@ -18,6 +18,7 @@
 
 package itdelatrisu.opsu.options;
 
+import fluddokt.ex.DevicePerformance;
 import fluddokt.opsu.fake.*;
 
 import itdelatrisu.opsu.Container;
@@ -140,7 +141,7 @@ public class Options {
 
 	/** The user agent to use in HTTP requests. */
 	public static final String USER_AGENT =
-		"opsu-android-revival/0.17.0-alpha1 (+https://github.com/PentagonGlxy/opsu-2)";
+		"opsu-android-revival/1.0.0 (+https://github.com/PentagonGlxy/opsu-2)";
 
 	/** The beatmap directory. */
 	private static File beatmapDir;
@@ -490,6 +491,15 @@ public class Options {
 				UI.resetFPSDisplay();
 			}
 		},
+		BATTERY_SAVER ("Battery saver", "BatterySaver",
+			"Use 60Hz in menus while keeping the fastest display mode for gameplay.\n" +
+			"The screen is kept awake only while actively playing.", true) {
+			@Override
+			public void toggle(GameContainer container) {
+				super.toggle(container);
+				DevicePerformance.info.setBatterySaverEnabled(bool);
+			}
+		},
 		SHOW_UNICODE ("Prefer metadata in original language", "ShowUnicode", "Where available, song titles will be shown in their native language (and character-set).", false) {
 			@Override
 			public void toggle(GameContainer container) {
@@ -1219,6 +1229,14 @@ public class Options {
 	 */
 	public static boolean isFPSCounterEnabled() { return GameOption.SHOW_FPS.getBooleanValue(); }
 
+	/**
+	 * Returns whether Android should use the adaptive battery-saving display profile.
+	 * @return true if adaptive battery saving is enabled
+	 */
+	public static boolean isBatterySaverEnabled() {
+		return GameOption.BATTERY_SAVER.getBooleanValue();
+	}
+
 	/**
 	 * Toggles the FPS counter display.
 	 */
diff --git a/src/itdelatrisu/opsu/states/Game.java b/src/itdelatrisu/opsu/states/Game.java
index eaaf8d69..fe14dacd 100644
--- a/src/itdelatrisu/opsu/states/Game.java
+++ b/src/itdelatrisu/opsu/states/Game.java
@@ -18,6 +18,7 @@
 
 package itdelatrisu.opsu.states;
 
+import fluddokt.ex.DevicePerformance;
 import fluddokt.opsu.fake.*;
 import fluddokt.newdawn.slick.state.transition.*;
 
@@ -1563,6 +1564,7 @@ public class Game extends BasicGameState {
 
 		if (beatmap == null || beatmap.objects == null)
 			throw new RuntimeException("Running game with no beatmap loaded.");
+		DevicePerformance.info.setGameplayActive(true);
 
 		// free all previously cached hitobject to framebuffer mappings if some still exist
 		FrameBufferCache.getInstance().freeMap();
@@ -1770,6 +1772,7 @@ public class Game extends BasicGameState {
 	@Override
 	public void leave(GameContainer container, StateBasedGame game)
 			throws SlickException {
+		DevicePerformance.info.setGameplayActive(false);
 //		container.setMouseGrabbed(false);
 
 		// re-hide cursor
