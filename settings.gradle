pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "opsu-android"

include "android", "core", "jlayer"

project(":core").buildFileName = "../gradle/core.gradle"
